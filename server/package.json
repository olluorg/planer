{
  "name": "thedad-sync-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "E2E-encrypted blob sync server for THEDAD (Fastify + Postgres)",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "pg": "^8.13.1"
  }
}
